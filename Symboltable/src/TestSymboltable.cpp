#include <iostream>

#include "Symboltable.h"
#include "LinkedList.h"
#include "../../TType.h"
using namespace std;

int main(int argc, char **argv) {

	Symboltable* symboltable;

	symboltable = new Symboltable();

	cout << "Symboltabellen-Tests beginnen hier\n";

	/*cout << "Größe:" << list->getSize() << endl;
	list->addLast(new Token ("in", identifier));
	cout << "Größe:" << list->getSize() << endl;
	list->addLast(new Token ("int", integer));
	cout << "Größe:" << list->getSize() << endl;

	cout << list->getToken(0)->lexem << "\n";
	cout << list->getToken(1)->lexem << "\n";*/

}
